Django==5.1.*
djangorestframework==3.15.*
PyJWT==2.*               # AMS login tokens (core/jwt.py, core/auth.py)
psycopg2-binary==2.9.*
django-cors-headers==4.4.*
python-decouple==3.8.*
bcrypt==4.2.*
reportlab==4.*
requests==2.34.*
beautifulsoup4==4.15.*
pdfplumber==0.11.*      # extract text from uploaded PDFs for AI summarization
python-docx==1.1.*      # extract text from uploaded .docx for AI summarization
truststore; sys_platform == "win32"  # optional: fixes gov SSL chains, import-guarded
# AI assistant uses an OpenAI-compatible LLM over the already-present `requests`.
# Backend is selectable via LLM_PROVIDER: 'local' (a local model over ngrok, LLM_BASE_URL)
# or 'gemini' (Google Gemini's OpenAI-compatible endpoint, GEMINI_API_KEY). No extra deps.

# --- Drafting (merged from InstaDraft; docs/merge-plan) ---------------------------
pgvector==0.3.*          # SampleClause / LibraryClause embeddings (drafting DB, schema "drf")
celery==5.4.*            # drafting background jobs (in-process until a worker is set up; docs/OPERATIONS.md)
redis==5.0.*
# AI stack for document parsing, embeddings, LLM calls and translation (~2 GB with torch).
anthropic==0.40.*
langchain==0.3.*
langchain-community==0.3.*
sentence-transformers==3.1.*
transformers>=4.49,<5
docling==2.111.*
numpy==2.2.*
einops==0.8.*
# IndicTransToolkit==1.1.*  # optional; translation uses NLLB-200 (transformers) without it
sentencepiece==0.2.*
sacremoses==0.1.*
